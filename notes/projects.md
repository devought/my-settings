## Project Queue

### Apps

- [ ] Telegram group chat game bot `Don't say the same thing as me`
- [ ] Telegram YES/NO questions game bot (1v1 with opponent)
- [ ] React Native app to train and refresh knowledge
- [ ] Online crypto trading system powered with AI
- [ ] Blazingly fast full stack web site
- [ ] Ugly UX components
- [ ] Portfolio 3D

### Under The Hood (JS/Web Engineering)

- [ ] Lexer + Parser + AST (tokenize and parse JS expressions)
- [ ] Interpreter (execute AST, like a mini JS engine)
- [ ] Transpiler (convert modern JS → older JS, like Babel)
- [ ] Bundler (resolve imports → dependency graph → single file, like Webpack/Rollup)
- [ ] Minifier (strip comments, rename vars via AST)
- [ ] Module system (implement CommonJS require() from scratch)
- [ ] Mini V8 (bytecode compiler + virtual machine + basic JIT)
- [ ] Mini SpiderMonkey (garbage collector + memory allocator)
- [ ] Mini WebKit layout engine (parse HTML + CSS → render tree → paint)
- [ ] Mini React (Virtual DOM + diff/patch + useState + useEffect)
- [ ] Mini TypeScript (type checker + type inference)
- [ ] Test runner (describe / it / expect / matchers, like Jest)
- [ ] Promise + Event Loop from scratch (no Promise/async, only callbacks)
- [ ] Observable (like RxJS, streams and operators)
- [ ] HTTP server on raw TCP (net.Socket, no frameworks)
- [ ] Router with radix tree (like Fastify)
- [ ] Task queue with workers and retry (like BullMQ)
- [ ] UI Component Library (button, input, modal, tooltip итд)
- [ ] CSS-in-JS runtime (like styled-components / emotion)
- [ ] State Manager (like Zustand / Redux)
- [ ] Reactive State (like MobX / Vue reactivity — Proxy based)
- [ ] Signal (like SolidJS / Angular signals)
